# macOS
.DS_Store

# dependencies
node_modules/

# Build output. The bundled plugin itself is committed: it is a single
# dependency-free file, and a user should be able to drop it into a plugins
# folder without installing Node.
dist/*
!dist/*.plugin.js
